languages:
  starlark:
    linters:
      - buildifier
    extensions:
      - .bzl
      - .bazel
    filenames:
      - BUILD
      - BUILD.bazel
      - MODULE.bazel
      - defs.bzl

linters:
  buildifier:
    enabled: true